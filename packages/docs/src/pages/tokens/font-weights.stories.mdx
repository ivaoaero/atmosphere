import { Meta } from '@storybook/addon-docs';
import { TokensGrid } from './../../components/TokensGrid';
import { fontWeights } from '@ivaoaero/tokens';

<Meta title="Tokes/Font Weights" />

# Font Weights

This text must be written in markdown and redone LOL. In short, this are the Font Weights tokens.

<TokensGrid tokens={fontWeights} />


